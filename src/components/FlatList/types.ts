import { StyleSheet } from 'react-native';

export const DEFAULT_FLATLIST_STYLES = StyleSheet.create({
  root: {
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    height: '100%',
    // paddingBottom: 100,
  },
});
